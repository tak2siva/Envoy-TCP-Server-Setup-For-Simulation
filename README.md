# Envoy-TCP-Server-Setup-For-Simulation

The docker compose has 2 services envoy and a TCP server in golang

This is to replicate some connection issues which are hard to reproduce otherwise and Fix it.
